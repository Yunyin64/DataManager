<UserControl x:Class="DataManager.Domain.LuaViewer.LuaViewerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="350">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 功能按钮栏（预留按钮 1、2、4、5） -->
        <Border Grid.Row="0" Background="#F0F0F0" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="4,3">
            <StackPanel Orientation="Horizontal">
                <Button Content="1" Width="28" Height="24" Margin="2,0" IsEnabled="False" ToolTip="预留功能 1"/>
                <Button Content="2" Width="28" Height="24" Margin="2,0" IsEnabled="False" ToolTip="预留功能 2"/>
                <Button Content="4" Width="28" Height="24" Margin="2,0" IsEnabled="False" ToolTip="预留功能 4"/>
                <Button Content="5" Width="28" Height="24" Margin="2,0" IsEnabled="False" ToolTip="预留功能 5"/>
            </StackPanel>
        </Border>

        <!-- 文件名标签 -->
        <Border Grid.Row="1" Background="#FAFAFA" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="8,4">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding FileName}" FontSize="12" FontWeight="SemiBold" Foreground="#333"/>
                <TextBlock Text=" ●" Foreground="#E67E22" FontSize="12" VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsDirty}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- AvalonEdit 编辑器 -->
        <avalonEdit:TextEditor x:Name="LuaEditor"
                               Grid.Row="2"
                               FontFamily="Consolas"
                               FontSize="13"
                               ShowLineNumbers="True"
                               WordWrap="False"
                               HorizontalScrollBarVisibility="Auto"
                               VerticalScrollBarVisibility="Auto"/>
    </Grid>
</UserControl>
