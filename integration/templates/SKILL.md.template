---
name: mytool-helper
description: <一句话说明：什么场景下 AI 应该主动加载这个 Skill。例如：游戏配置数据库助手，涉及策划表的查看、搜索、修改时使用。>
---

# <你的工具名> 助手

<两三句话介绍角色和职责。例如："你是一位熟悉 XXX 的助手，帮助用户用自然语言操作 YYY..."。
告诉 AI 它应该用什么风格回答用户、有什么基本原则。>

---

## 基本原则

**先搞清楚再动手**
- <列出做事前要确认的事项>

**结果要说人话**
- <列出回复时的格式要求，例如：不要直接贴原始数据，整理成表格；字段名双语显示；告诉用户找到了几条记录等>

---

## CLI 工具

`common-cli.exe` 已加入系统 PATH，可直接调用。

### 命令格式

```bash
common-cli.exe MyTool <WorkspaceId> <command> [args...]
```

`<WorkspaceId>` 用于指定要连接的工作区。可以先用 `auto` 自动连接：

```bash
common-cli.exe MyTool auto <command> [args...]
```

- 如果只有一个工作区，`auto` 会直接连接并执行命令。
- 如果失败，返回的 JSON 中会包含 `message` 字段说明原因：
  - 没有工作区 — 提醒用户打开 <你的工具名>。
  - 多个工作区 — JSON 中的 `workspaces` 数组列出所有可用工作区的 `id` 和 `description`，
    展示给用户，询问要操作哪个，然后用具体的 `id` 替换 `auto` 重新执行。

### 连接错误处理

如果 `common-cli` 返回 **"Failed to connect to tool"** 相关错误，说明 <你的工具名> 程序已关闭
或该工作区不可用。此时必须**立即停止**，提醒用户检查工具是否正在运行，等待用户确认后再继续。

不要在连接失败后自动重试。

### 命令清单

| 命令 | 用途 | 示例 |
|---|---|---|
| `ping` | 健康检查 | `ping` |
| `<cmd1>` | <说明> | `<示例>` |
| `<cmd2>` | <说明> | `<示例>` |

### stdin 传递最后一个参数

当最后一个参数包含复杂内容（如含特殊字符）或太长可能超过命令行长度上限时，
可通过 heredoc 从 stdin 传入：

```bash
common-cli.exe MyTool <WorkspaceId> <command> <<'EOF'
这里是大段内容
可以多行
EOF
```

所有命令均支持此特性，stdin 内容会作为最后一个参数追加。

---

## 命令详细说明

### `<cmd1>` — <一句话说明>

<详细说明：参数、选项、返回格式、示例>

```bash
common-cli.exe MyTool <WorkspaceId> <cmd1> [options] <required-arg>
```

#### 选项

| 选项 | 说明 |
|---|---|
| `-x, --xxx <value>` | <说明> |
| `-f, --flag` | <说明> |

#### 示例

```bash
common-cli.exe MyTool auto <cmd1> --xxx foo bar
```

#### 返回格式

<说明 JSON 结构或文本格式>

---

### `<cmd2>` — <一句话说明>

<同上>
